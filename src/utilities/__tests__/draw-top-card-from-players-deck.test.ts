import { Zone } from '../../interfaces';
import createZoneObject from '../create-zone-object';
import { drawTopCardFromPlayersDeck } from '..';

describe('Handles moving card entries from G.Deck to G.Hands', () => {
  test('Should ', () => {
    // const fn = drawTopCardFromPlayersDeck(Zones);
    // expect(fn).toEqual('0');
  });
});
